# Required environment variables for WOPR seed files
#
# Linear API key — used by the linear-wopr source for webhook authentication
LINEAR_API_KEY=lin_api_XXXXXXXX

# Defcon URL — the defcon instance that receives flow definitions
DEFCON_URL=http://localhost:4000
